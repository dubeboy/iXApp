<nav class="navbar navbar-expand-lg navbar-light bg-light pad-it">
  <div class="container">
    <a class="navbar-brand" href="#">iX Link</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <ul class="navbar-nav">
      <%if current_student %>
    <li class="nav-item ">
          <a class="nav-link" href="#">Logged in as: <%= current_student.email %></a>
    </li>

        <li>
          <%= link_to 'Sign out', destroy_student_session_path, method: :delete, class: "btn btn-danger" %>
        </li>
        <% else %>
        <li>
          <%= link_to 'Sign In', new_student_session_path %>
        </li>
    <% end %>
    </ul>
  </div>
</nav>
